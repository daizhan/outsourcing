define(["layer", "utils", "color-picker", "popup-menu"], function(layer, utils, colorPicker, popupMenu) {
    return {
        layer: layer,
        utils: utils,
        colorPicker: colorPicker,
        popupMenu: popupMenu
    }
});